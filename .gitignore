# Rust
/target
**/target/
**/*.rs.bk
!/Cargo.lock
# proptest regression files (generated at test time; not committed per proptest convention)
proptest-regressions/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
*.whl
.venv/
venv/

# Beava-specific
.thoughts/
deck/
*.snapshot*
events/
beava.snapshot*
# Runtime WAL/snapshot dirs that tests may create in the repo root
**/beava-wal/
**/beava-snapshots/

# Marketing / pitch artifacts (not part of the OSS tree)
deck-*.html
deck-*.pdf
*_Deck.pdf
Tally_VAS2026_Deck.pdf
branding*.png
beaver*.png
!site/assets/branding*.png
2026-*-outreach-*.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*

# Benchmark results
benchmark/**/results/

# OS
.DS_Store
Thumbs.db

# Claude Code (mostly excluded from public OSS repo) — but ship the
# project-specific PR review skill so contributors using Claude Code
# get the same review surface as maintainers.
/.claude/*
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/beava-pr-review/

# MCP server config (personal, points to local gbrain instance)
.mcp.json
.gstack/

# Beava website build artifacts
beava-website/node_modules/
render-docs-warnings.txt
beava-website/link-audit-report.txt

# IDE configs (additional)
.cursor/

# OS metadata (additional)
._*
Desktop.ini

# Editor backups / generic temp
*.bak
*.tmp
.cache/

# Logs
*.log
tmp/

# Python type-check + lint caches
.mypy_cache/
.ruff_cache/

# JS/TS (global; sdk/typescript also has its own .gitignore)
node_modules/
*.tsbuildinfo

# Go (defensive — Go SDK in Phase 13.6 roadmap)
vendor/
*.test

# Coverage / profiling
.coverage
coverage.xml
htmlcov/
*.lcov
flamegraph.svg
*.trace/
*.trace

# Local secrets (additional)
*.pem
*.key
secrets/

# Beava runtime data dir (post-12.9 convention)
.beava/

# === v2/greenfield cleanup: untrack legacy + personal artifacts ===
# Launch / pitch / outreach drafts — kept locally, not in public OSS repo
/LAUNCH*.md
/BLOG-*.md
/SENDO-*.md
/realtime-pain-points.*

# v1 design / governance docs (superseded; kept local only)
/DESIGN-V2.md
/MAINTAINERS.md
/GOVERNANCE.md

# Misc one-offs not part of the public tree
/demo.py
/beava.png

# Legacy v1 top-level trees — workspace lives in crates/ now; tests under
# python/tests/ + crates/*/tests/; benches under crates/*/benches/.
/src/
/tests/
/benches/
/benchmark/

# Local-only marketing / design work (deferred from public OSS)
/launch/
/site/

# Conversation transcripts inside the website / design-system trees
# (tracked source lives under project/, not chats/).
/beava-website/chats/
/beava-design-system/chats/

# Internal-only tooling and planning state (kept local, not in public OSS)
/.planning/
/.agents/
/CLAUDE.md
/scripts/

# SDKs deferred from public OSS for now (Python SDK ships under python/)
/sdk/go/
/sdk/typescript/

# Examples deferred from public OSS for now
/examples/

# Legacy v1 Python SDK (`tally`) and its deprecation-contract tests.
# `python/beava/` is the v2 SDK and ships standalone; tally is dead weight.
/python/tally/
/python/tests/test_client.py
/python/tests/test_fork_unit.py
/python/tests/test_get_multi_e2e.py
/python/tests/test_operators.py
/python/tests/test_protocol.py
/python/tests/test_suggest_config_cli.py
/python/tests/test_ttl_defaults.py
/python/tests/test_v0_col.py
/python/tests/test_v0_dag.py
/python/tests/test_v0_decorators.py
/python/tests/test_v0_public_surface.py
/python/tests/test_v0_register_roundtrip.py
/python/tests/test_v0_schema.py
/python/tests/test_v0_serialize.py
/python/tests/test_v0_stateless_ops.py
/python/tests/test_v0_validate.py
